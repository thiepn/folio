name: Windows runner probe

on:
  pull_request:

permissions:
  contents: read

jobs:
  probe:
    runs-on: windows-latest
    timeout-minutes: 3
    steps:
      - run: node --version
      - run: echo "Windows runner available"
